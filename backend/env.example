# ============================================
# REQUIRED ENVIRONMENT VARIABLES (Production)
# ============================================

# MongoDB Atlas connection string (REQUIRED)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?retryWrites=true&w=majority

# JWT Secret Key (REQUIRED) - Use a strong, random secret in production
JWT_SECRET=your_super_secret_key_change_this_in_production

# CORS - Frontend URLs (REQUIRED in production, comma-separated)
# Example: https://your-frontend.onrender.com,https://yourdomain.com
CORS_ORIGINS=https://multiomic-data-orchestrator-fe.onrender.com

# ============================================
# OPTIONAL ENVIRONMENT VARIABLES
# ============================================

# JWT Token expiration time in minutes (default: 60)
JWT_EXPIRES_IN=60

# Database name (default: mdo)
DATABASE_NAME=mdo

# Port for the server (default: 8000, Render provides this automatically)
PORT=8000

# Seed User Configuration (Optional - for initial admin user)
# Only set these if you want to auto-create a seed user on startup
SEED_USER_EMAIL=admin@example.com
SEED_USER_PASSWORD=change_this_password
SEED_USER_NAME=Admin


